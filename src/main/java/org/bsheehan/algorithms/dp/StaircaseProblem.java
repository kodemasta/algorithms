package org.bsheehan.algorithms.dp;

/**
 * Max permutations of reaching a step given M max size step (e.g. 2 (one or two steps))
 * # of ways is Fibonocci series as we incremement along steps
 *
 * M = 2 ==> f(n) = f(n-1) + f(n-2)
 * M = 3 ==> f(n) = f(n-1) + f(n-2) + f(n-3)
 */
public class StaircaseProblem {
}
